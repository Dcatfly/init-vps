version: '1'

services:
  shadowsocks:
    image: shadowsocks/shadowsocks-libev
    ports:
      - "12137:8388"
    environment:
      - METHOD=aes-256-gcm
      - PASSWORD=password
    restart: always